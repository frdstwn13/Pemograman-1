#include <stdio.h>

int main(){
    char nama[50], nim[50], kelas[50], tempatlahir[50], alamat[50], hobby[50], nohp[50];

printf("Nama                      :");
gets(nama);

printf("NIM                       :");
gets(nim);

printf("Kelas Paralel             :");
gets(kelas);

printf("Tempat/Tanggal Lahir      :");
gets(tempatlahir);

printf("Alamat                    :");
gets(alamat);

printf("Hobby                     :");
gets(hobby);

printf("No. Hp                    :");
gets(nohp);
    
   return 0;

}